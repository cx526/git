### git clone https://github.com/cx526/test.git
### git add .
### git commit -m "explain"
### git push
### git pull 拉去最新代码
### 文件再次被修改后覆盖


### git branch 检查分支
### git branch dev 创建dev分支
### git checkout dev 切换到dev分支
### git merge dev 合并dev分支到master上(切换到master分支上合并)
### git branch -d dev 删除dev分支，删除的分支当前不能被使用

### .gitignore可以忽略掉某些文件
